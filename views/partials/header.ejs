<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <link rel="stylesheet" href="/styles/layout.css">
  <link rel="stylesheet" href="/styles/content.css">
  <title>Altair Blog</title>
</head>

<body>
  <header>
    <nav class="navbar navbar-expand-custom navbar-mainbg">
      <a class="navbar-brand navbar-logo" href="/">HOME</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <i class="fas fa-bars text-white"></i>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
          <div class="hori-selector">
            <div class="left"></div>
          </div>
          <li class="nav-item">
            <a class="nav-link" href="/add_new_user"><i class="fa-solid fa-user-plus"></i>JOIN US</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/search"><i class="fas fa-search"></i>SEARCH</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/job_vacancies"><i class="fas fa-briefcase"></i>VACANCIES</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/new_clinic_stories"><i class="fas fa-clinic-medical"></i>POST</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/market"><i class="fas fa-store"></i>MARKET</a>
          </li>
        </ul>
      </div>
    </nav>
  </header>

<!-- Flash Notifications -->

 <div
  id="flash-container"
  class="container"
  style="position: fixed; top: 70px; left: 50%; transform: translateX(-50%); z-index: 9999;"
>
  <% if (success_msg && success_msg.length > 0) { %>
      <div class="alert alert-success alert-dismissible fade show text-center" role="alert">
        <%= success_msg %>
        <button type="button" class="close" data-dismiss="alert">
          <span>&times;</span>
        </button>
      </div>
    <% } %>

    <% if (error_msg && error_msg.length > 0) { %>
      <div class="alert alert-danger alert-dismissible fade show text-center" role="alert">
        <%= error_msg %>
        <button type="button" class="close" data-dismiss="alert">
          <span>&times;</span>
        </button>
      </div>
    <% } %>
  </div>



  <script>
    setTimeout(() => {
      document.querySelectorAll('#flash-container .alert').forEach(alert => {
        alert.classList.remove('show');
      });
    }, 4000);
  </script>

<!--main class="container"><!-- Main content starts here -->   